version: '3.8'

services:
  web:
    build: .
    command: gunicorn agriyield.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/app
      - ./db.sqlite3:/app/db.sqlite3
      - ./media:/app/media
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
